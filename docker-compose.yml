services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: livewarden
      POSTGRES_USER: livewarden
      POSTGRES_PASSWORD: livewarden
    ports:
      - "5433:5432"
    volumes:
      - livewarden-postgres:/var/lib/postgresql/data

volumes:
  livewarden-postgres:
