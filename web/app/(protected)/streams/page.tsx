import { StreamManagement } from '../../../components/streams/stream-management';

export default function StreamsPage() { return <section aria-labelledby="streams-heading"><StreamManagement /></section>; }
